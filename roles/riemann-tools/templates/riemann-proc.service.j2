[Unit]
Description=Send Process statistics to Riemann
After=network.target

[Service]
User=monitoring
ExecStart=/home/monitoring/.gem/ruby/{{ ruby_version }}/bin/riemann-proc --host {{ groups['riemann'][0] }} --event-host {{ inventory_hostname }}
Restart=always
