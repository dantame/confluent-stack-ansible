[Unit]
Description=Send CPU Memory Disk statistics to Riemann
After=network.target

[Service]
User=monitoring
ExecStart=/home/monitoring/.gem/ruby/{{ ruby_version }}/bin/riemann-health --host {{ groups['riemann'][0] }} --event-host {{ inventory_hostname }}
Restart=always
