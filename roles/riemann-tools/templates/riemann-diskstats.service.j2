[Unit]
Description=Send disk usage statistics to Riemann
After=network.target

[Service]
User=monitoring
ExecStart=/home/monitoring/.gem/ruby/{{ ruby_version }}/bin/riemann-diskstats --host 127.0.0.1
Restart=always
